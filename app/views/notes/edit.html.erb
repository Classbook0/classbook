<h1>Editar Nota</h1>
<h2>Curso: <%= @curso ? @curso.nombre : ''%></h2>
<%= render 'form_up' %>

<%= link_to 'Ver', curso_note_path(params[:curso_id],params[:id]) %> |
<%= link_to 'Volver', curso_notes_path(params[:curso_id]) %>
